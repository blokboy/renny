import { ConvocationMap } from "@/components/convocation/ConvocationMap";

export default function ConvocationPage() {
  return (
    <main className="fixed inset-0 bg-black">
      <ConvocationMap />
    </main>
  );
}
